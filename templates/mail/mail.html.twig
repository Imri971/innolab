
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" >
 
<style>
        th{
            margin: 1rem auto 1rem;
            color: #000;
        }
    </style>
</head>
<body>
    <div class="container">
            
            <h3>Vous avez reçu une demande de contact </h3>
            <small>Vous avez reçu un mail le {{date | date('d/m/Y')}} à {{date | date('H:i')}} </small>
            
            <p>Vous avez reçu un message depuis votre site par <strong>{{name}}</strong> </p>
            <p>Authentifié par {{email}}</p>
            <p><strong>Message:</strong> {{message}}</p>
            {# <table class="table">
                <tbody class="text-center">
                    <tr>
                        <th> <i class="far fa-user-circle"></i> Name</th>
                        <td>{{name}}</td>
                    </tr>
                    <tr>
                        <th> <i class="fas fa-envelope"></i> Email</th>
                        <td>{{email}}</td>
                    </tr>
                    <tr>
                        <th colspan="2" class="mt-4"> <i class="fas fa-comments"></i> Message</th>
                    </tr>
                    <tr>
                        <td colspan="2" class="mt-4" >{{message}}</td>
                    </tr>
                </tbody>
            </table> #}

    </div>
</body>
</html>