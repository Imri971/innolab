{% extends 'base.html.twig' %}

{% block title %}Programmes{% endblock %}

{% block body %}
<div class="col-lg-10 ml-auto mr-auto mt-4">
    <h1>Programme {{ programme.name }}</h1>

    <table class="table text-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ programme.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ programme.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ programme.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('programmes_index') }}" class="btn btn-prus mb-2">Retour à la liste</a>

    <a href="{{ path('programmes_edit', {'id': programme.id}) }}" class="btn btn-prus mb-2">Modifier</a>

    {{ include('programmes/_delete_form.html.twig') }}
</div>
{% endblock %}